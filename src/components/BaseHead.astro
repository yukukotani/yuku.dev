---
export interface Props {
  title?: string;
  url: string;
  ogImage?: string;
}
const { title, url, ogImage } = Astro.props;
const fullTitle = title ? `${title} | Yuku Kotani` : "Articles | Yuku Kotani"
const description = "こたにゆうくのブログ";
---
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />

<link rel="icon" type="image/png" href="/icon.png" />

<!-- Primary Meta Tags -->
<title>{fullTitle}</title>
<meta name="title" content={fullTitle} />
<meta name="description" content={description} />

<!-- Open Graph / Facebook -->
<meta property="og:type" content="website" />
<meta property="og:url" content={url} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImage || "https://yuku.dev/icon.png"} />

<!-- Twitter -->
<meta property="twitter:card" content={ogImage ? "summary_large_image" : "summary"} />
<meta property="twitter:url" content={url} />
<meta property="twitter:creator" content="@MonchiFC" />
<meta property="twitter:site" content="@MonchiFC" />

<!-- Stylesheet -->
<link rel="stylesheet" href={Astro.resolve('../styles/global.scss')} />
<link rel="stylesheet" href="https://unpkg.com/modern-css-reset@1.4.0/dist/reset.min.css" />

<!-- Analytics -->
{ process.env.VERCEL_ENV === "production" ? 
  <script async src={`https://www.googletagmanager.com/gtag/js?id=G-Y6GFJZCCEN`} />
  : null }
{ process.env.VERCEL_ENV === "production" ?
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-Y6GFJZCCEN', {
      page_path: window.location.pathname,
    });
  </script>
  : null }